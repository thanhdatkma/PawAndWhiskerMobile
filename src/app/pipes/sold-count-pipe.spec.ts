import { SoldCountPipe } from './sold-count-pipe';

describe('SoldCountPipe', () => {
  it('create an instance', () => {
    const pipe = new SoldCountPipe();
    expect(pipe).toBeTruthy();
  });
});
